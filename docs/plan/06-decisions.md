
## 2026-07-05 · 밀키트로 이사
- 결정: 건강가이드북을 flower 밀키트(초기 버전) 복사본 위에서 진행한다.
- 왜: 부트캠프 도구(/blueprint 등 스킬·훅)와 배포 경로를 그대로 쓰기 위해.
- 버린 대안: 단일 html 폴더 그대로 진행 (도구 없이).
- 영향: 기존 앱은 prototype/index.html 로 보관, 지도는 docs/plan/00-guidebook-map.md, 원본 폴더는 ../health-guide-prototype 에 백업.

## 2026-07-10 · 프로토타입을 apps/web(Next.js)으로 이식
- 결정: prototype/index.html 의 동작을 apps/web(Next.js/React)으로 그대로 옮기고, 앞으로 블럭 1~10 은 여기서 만든다.
- 왜: 밀키트로 이사(위 결정)해놓고 실제 코드는 벽보(html)에 있었다. 지금 옮길 게 424줄뿐이라 가장 쌈. 배포·가족계정(Supabase)이 여기서만 붙는다.
- 버린 대안: 프로토타입 html 에서 계속 만들다 나중에 이식 (버리는 작업이 커짐).
- 영향: apps/web/app/{page.tsx, layout.tsx, globals.css, lib/health.ts} 채움. prototype/index.html 은 백업으로 보존. 저장은 아직 localStorage 그대로.

## 2026-07-10 · 빈혈 항목 확장하지 않음
- 결정: 빈혈 입력은 혈색소(Hb) 한 칸으로 유지하고, 철분 패널(페리틴·혈청철·TIBC 등)은 추가하지 않는다.
- 왜: 중장년 검진에서 빈혈은 최우선 관심사가 아님(혈관·혈당·간·콩팥·비만이 상위). 우선순위 낮은 곳에 화면·복잡도를 쓰지 않는다.
- 버린 대안: 실제 검진지의 철분/골다공증 항목까지 담도록 입력표 확장.
- 영향: 입력표·판정 로직 변화 없음. 결과지에 Hb 없으면 그 칸은 비워둔다.

## 2026-07-10 · 부분 입력 허용 (가진 수치만 넣어도 결과)
- 결정: 검진지에 있는 수치만 넣어도 결과가 나오게 하고, 안 넣은 항목은 '미입력'으로 부드럽게 안내한다(에러 아님).
- 왜: 결과지마다 항목이 달라 빈칸이 생긴다. 빈칸 때문에 막히지 않고 가진 것만으로 결과를 얻게 하는 게 사용자 니즈.
- 버린 대안: 모든 칸을 채워야 결과가 나오는 방식.
- 영향: 입력 화면에 안내 문구 추가, Part 2 결과 하단에 '아직 안 넣은 항목 N개' 안내 + 채우러 가기. 저장·판정 로직은 그대로(빈칸은 이미 건너뜀).

## 2026-07-11 · 근거 밝히기 (판정 기준 출처 + 점수 성격 안내)
- 결정: 판정(정상/주의/위험) 경계값의 출처를 문서화하고, Part 2 하단에 "점수는 요약이며 진단 아님" 안내문을 상시 표시한다.
- 왜: 건강 앱은 근거가 신뢰의 핵심. 판정 기준은 학회·국가검진 기준(근거 있음)이지만, 점수·별점은 우리 앱의 요약 방식(의학 표준 아님)이라 이를 정직하게 밝혀야 한다.
- 버린 대안: 근거 없이 점수만 보여주기 / 나중으로 미루기.
- 영향: docs/plan/기준-근거.md 신규. Part 2 하단 .disclaimer 안내문 추가(globals.css + page.tsx). 판정·점수 로직은 변경 없음.

## 2026-07-14 · Part 9 규칙 기반 먼저, AI는 키 발급 후
- 결정: Part 9(미래 질환 갈림길)를 규칙 기반으로 먼저 만들고, AI 연결(Part 1 편지·Part 9 업그레이드)은 Anthropic API 키 발급 후 진행.
- 왜: .env의 ANTHROPIC_API_KEY가 dummy(가짜) — 실제 호출엔 유료 키 필요. 돈·가입이 걸린 결정이라 사용자 선택으로.
- 버린 대안: 지금 바로 키 발급받고 AI 연결.
- 영향: buildRisks(규칙 기반) 추가로 Part 9 완성. AI 붙일 때 글 생성만 교체하면 되는 구조.

## 2026-07-16 · 깃허브 백업 + 버셀 배포 완료
- 결정: GitHub Desktop으로 푸시 경로 개통, 버셀에 apps/web 배포 (Root Directory=apps/web, 환경변수 없음).
- 왜: 2주치 작업물이 노트북에만 있던 위험 해소 + 가족 폰에서 접속할 인터넷 주소 확보.
- 버린 대안: 명령줄 인증(gh/토큰) — 초보자에게 GitHub Desktop이 가장 쉬움.
- 영향: 공식 주소 https://health-guide-web.vercel.app 탄생. 앞으로 "클로드 커밋 → 사용자 Push origin 클릭 → 버셀 자동 재배포" 흐름.
